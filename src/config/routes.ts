const routes = {
  gettingStarted: '/',
  sign: '/sign',
  decrypt: '/decrypt',
  records: '/records',
  transfer: '/transfer',
  execute: '/execute',
  deploy: '/deploy',
  transitionViewKey: '/transition-view-key',
};

export default routes;
